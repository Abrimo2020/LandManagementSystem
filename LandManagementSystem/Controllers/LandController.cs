using LandManagementSystem.Models;
using Microsoft.AspNetCore.Mvc;

namespace LandManagementSystem.Controllers
{
    public class LandController : Controller
    {
        //public IActionResult Index()
        //{
        //    // Retrieve a list of land parcels from your data source
        //    List<LandModel> landParcels = GetLandParcelDataFromSource();

        //    return View(landParcels);
        //}
        //// Add actions for adding, editing, and deleting land parcels

        //private List<LandModel> GetLandParcelDataFromSource()
        //{
        //    // Retrieve land parcel data from your data source (e.g., database)
        //    // Convert the data to a list of LandModel objects and return it
        //}
    }
}
